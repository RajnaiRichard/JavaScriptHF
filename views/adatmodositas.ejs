<%- include('header') -%>

<div class="container">
    <div  class="row h-100 d-f lex align-items-center justify-content-center" >
        <div class="col-12 col-md-4" >
            <h2>Modositas</h2>
            <form method="POST">
                <input type="hidden" name="id" value="<%=(typeof user === 'undefined')?'':user._id%>"/>
                <div class="form-group">
                    <label for="azonosito">Azonosito:</label>
                    <input type="text" class="form-control" id="azonosito" placeholder="Azonosito" name="azonosito" value="<%=(typeof user === 'undefined')?'':user.Nev%>">
                </div>
                <div class="form-group">
                    <label for="jelszo">Jelszo:</label>
                    <input type="password" class="form-control" id="jelszo" placeholder="Jelszo" name="jelszo">
                </div>
                <input type="submit" class="btn btn-primary" value="Mentes"/>
                <a href="/" type="submit" class="btn btn-link">Megse</a>
                <a href="/torles/<%=(typeof user === 'undefined')?'':user._id%>" type="submit" class="btn btn-danger float-right">Fiok torlese</a>
                
            </form>

    </div>
</div>
</div>

    <%- include('hibak') -%>
    <%- include('footer') -%>